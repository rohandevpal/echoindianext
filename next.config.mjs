/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
        domains: ['http://localhost:3000/'], // Add your allowed domains here
    },
};

export default nextConfig;


// next.config.js